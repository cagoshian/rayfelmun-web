import SectionTitle from "../Common/SectionTitle";

const Gallery = () => {
  return (
    <section id="gallery">
            <SectionTitle
              title="Photo Gallery"
              paragraph="Photos will be shared here after the conference."
            ></SectionTitle>
    </section>
  );
};

export default Gallery;
